#### IP packet routing using Trie
Uses a Trie to store the routing table and find the longest prefix match for a given IP address to determine the router to send the packet to.

#### Usage
```
g++ main.cpp -o main
./main
```